/*
 * Imported straight from sessionOutput.ts, never src/pdfdocumenteditor's barrel, which
 * would bring the whole PDF stack into the initial chunk.
 */
export {
  documentEditorSessionAfterSave,
  documentEditorSessionOutput,
} from "../pdfdocumenteditor/sessionOutput";
