node_modules
dist
.generated
package-lock.json
coverage
playwright-report
test-results

# Vendored and user content: never ours to reformat.
*.pdf
src/browserapp/assets

# The vendored fleet files. Not ignored because they are generated — they are tracked source —
# but because this project does not OWN them: they are pasted, byte-identical, into every carrier
# in the fleet, and `npm run format` rewriting one here would fork this copy from the other ten.
# Measured: with these lines absent, a formatting break planted in theme.css is reported by
# `format:check`, so prettier does see them and .gitignore does not cover them.
src/theme.css
scripts/stamp_version.mjs

# Generated from the dependency tree; reformatting it would fork it from what
# the generator emits.
THIRD-PARTY-NOTICES.md
